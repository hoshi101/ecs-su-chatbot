# ECS Chatbot Environment Configuration

# === REQUIRED: Core API Keys ===
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_URL=your_qdrant_url_here  # e.g., https://xyz.qdrant.io:6333
GOOGLE_API_KEY=your_google_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
TAVILY_API_KEY=your_tavily_api_key_here

# === Department Domain Configuration ===
DOMAIN_NAME=Department of Electrical Engineering, Silpakorn University
BOT_NAME=น้องไฟฟ้า (ECS AI Assistant)
BOT_NAME_EN=N' Faifa
SEARCH_DOMAINS=ee-eng.su.ac.th,eng2.su.ac.th

# === Assistant Intelligence Settings ===
# Enable/disable query enhancement (default: true)
ENABLE_QUERY_ENHANCEMENT=true
# Default LLM provider: gemini or openai
LLM_PROVIDER=openai
# Default provider models
GEMINI_MODEL=gemini-2.5-flash
OPENAI_MODEL=gpt-5.4-mini
# Similarity threshold for document relevance (default: 0.7)
DEFAULT_SIMILARITY_THRESHOLD=0.7
# Response temperature (default: 0.3)
FINANCIAL_TEMPERATURE=0.3

# === Vector Database Configuration ===
QDRANT_COLLECTION_NAME=ecs-su-chatbot-bge-m3 # use your qdrant collection name
EMBED_MODEL=BAAI/bge-m3
# BGE-M3 embedding dimensions (do not change)
EMBED_DIMENSIONS=1024

# === Document Processing ===
DOC_SOURCE_DIR=data

# === Rate Limiting Configuration ===
# Enable/disable rate limiting (default: true)
RATE_LIMIT_ENABLED=true
# Maximum requests per minute per IP (default: 10)
RATE_LIMIT_PER_MINUTE=10
# Rate limit strategy: fixed-window or moving-window (default: fixed-window)
RATE_LIMIT_STRATEGY=fixed-window
